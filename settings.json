{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:live-demo}/data/event.db",
            "name": "eventdb"
        }
    ]
}